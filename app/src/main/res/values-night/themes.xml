<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema base en modo oscuro (Dark por defecto) -->
    <style name="Theme.UniversalInstaller" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorPrimaryContainer">@color/primary_container</item>
        <item name="colorOnPrimaryContainer">@color/on_primary_container</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/on_surface</item>
        <item name="colorSurfaceVariant">@color/surface_variant</item>
        <item name="colorOnSurfaceVariant">@color/on_surface_variant</item>
        <item name="android:statusBarColor">@color/surface</item>
        <item name="android:navigationBarColor">@color/surface</item>
        <item name="android:windowLightStatusBar">false</item>
        <!-- Transiciones suaves entre actividades -->
        <item name="android:windowActivityTransitions">true</item>
        <item name="android:windowContentTransitions">true</item>
    </style>

    <!-- Tema transparente (hereda del base) -->
    <style name="Theme.UniversalInstaller.Transparent" parent="Theme.UniversalInstaller">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <!-- Tema AMOLED (negro puro) -->
    <style name="Theme.UniversalInstaller.AMOLED" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/oled_primary</item>
        <item name="colorOnPrimary">@color/oled_on_primary</item>
        <item name="colorPrimaryContainer">@color/oled_primary_container</item>
        <item name="colorOnPrimaryContainer">@color/oled_on_primary_container</item>
        <item name="colorSurface">@color/oled_surface</item>
        <item name="colorOnSurface">@color/oled_on_surface</item>
        <item name="colorSurfaceVariant">@color/oled_surface_variant</item>
        <item name="colorOnSurfaceVariant">@color/oled_on_surface_variant</item>
        <item name="android:statusBarColor">@color/oled_surface</item>
        <item name="android:navigationBarColor">@color/oled_surface</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowActivityTransitions">true</item>
        <item name="android:windowContentTransitions">true</item>
    </style>

</resources>
